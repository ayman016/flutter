import 'package:flutter/material.dart';

class ScreenHome extends StatefulWidget {
  const ScreenHome({super.key});

  @override
  State<ScreenHome> createState() => _ScreenHomeState();
}

class _ScreenHomeState extends State<ScreenHome> {
  GlobalKey<FormState> fomstate = GlobalKey();
  String? username;
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('AppBar أنيق'),
        centerTitle: true,
        elevation: 8, // ظل واضح ولكن ناعم
        backgroundColor: Colors.pink,
        foregroundColor: Colors.black, // باش يكون التيتر باين
        shape: RoundedRectangleBorder(
          borderRadius: BorderRadius.vertical(
            bottom: Radius.circular(30), // زوايا سفلية مدورة
          ),
        ),
      ),
    );
  }
}




With gradient:



appBar: AppBar(
        title: Text('AppBar ب Gradient'),
        centerTitle: true,
        elevation: 8,
        backgroundColor: Colors.transparent, // مهم باش يبان الـ gradient
        foregroundColor: Colors.white,
        shape: RoundedRectangleBorder(
          borderRadius: BorderRadius.vertical(bottom: Radius.circular(30)),
        ),
        flexibleSpace: Container(
          decoration: BoxDecoration(
            gradient: LinearGradient(
              colors: [Colors.deepPurple, Colors.indigo, Colors.blueAccent],
              begin: Alignment.topLeft,
              end: Alignment.bottomRight,
            ),
            borderRadius: BorderRadius.vertical(bottom: Radius.circular(30)),
          ),
        ),
      ),